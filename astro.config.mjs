// @ts-check
import { defineConfig } from 'astro/config';

// TODO: replace with the real production domain once it's registered/connected
// in Cloudflare Pages. Until then, OG tags, canonical URLs, and the sitemap
// below will all point at this placeholder instead of the live domain.
export default defineConfig({
	site: 'https://bridgetbeattyconsulting.com',
});
